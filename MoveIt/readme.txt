To install the dependecies (one time installation):
- Open a Terminal and cd to the directory of the move_it.py script
- Run the following cmd to install the dependencies
	pip install -r requirements.txt
- Run the following cmd everytime to run the script 
	move_it.py 